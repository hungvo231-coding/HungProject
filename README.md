# HungProject
//  Project demonstrates how to:
//    Define a struct named Item containing a string name and an int id.
//    Dynamically allocate an array of Items using new.
//    Populate the array with at least 100 sample records sorted by name.
//    Create a binary search function to find an item with its ID.
//    Ask user input ID to search.
//    Display the results
//    Free memory using delete[]
